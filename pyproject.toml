[project]
name = "manimations"
version = "0.1.0"
description = "AI-driven pipeline that converts quantum prompts into Manim animations."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "manim>=0.20.1",
    "rich>=13.4.0",
    "unsloth>=0.0.0",
    "torch>=2.0.0",
    "qwen-vl-utils>=0.0.14",
]
